# See man gsimplecal for other options

# Setting to 0 prevents i3 from tiling the window.
mainwindow_resizable = 0
# Temporary workaround until sort out positioning by the bottom right that is
# entirely within the screen
mainwindow_position = center
